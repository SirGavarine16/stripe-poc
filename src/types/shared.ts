export type StoreURL = {
  label: string;
  href: string | null;
};

export type ProductData = {
  name: string;
  image: string;
  brand: string;
  category: string;
  description: string;
  price: number;
  currency: string;
};
